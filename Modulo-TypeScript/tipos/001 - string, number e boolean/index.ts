let pedido: string = "Pizza";
pedido = "Miriã Amaral"

let numero: number = 1;
numero = 2;

let deuBom: boolean = false;
deuBom = true;

const fun = (value: string): string => {
    return value;
};

console.log(fun("Miriã Amaral"));

// string, number e boolean são tipos primitivos em TypeScript. 
// Eles são usados para representar valores de texto, números e 
// valores booleanos (verdadeiro ou falso), respectivamente. 
// Esses tipos são fundamentais para a construção de programas em TypeScript, 
// permitindo que os desenvolvedores definam variáveis e funções com tipos específicos, 
// garantindo maior segurança e previsibilidade no código.